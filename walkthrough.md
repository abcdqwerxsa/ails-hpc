# AILS HPC 面板使用手册（实验室成员版）

> 面板地址：https://portal.nilpo.app/portal/ （推荐；内网备入口 http://192.168.20.226:8090/portal/ ——Web-IDE 仅域名入口可用）。本文只讲"怎么用"；
> 架构与运维见 PROJECT.md / docs/（面向开发者与管理员）。

## 1. 登录与账号

- 用管理员分配的**用户名 + 初始密码**登录。初始密码或被管理员重置后，**首次登录会强制要求修改密码**（自助改密页），策略：至少 8 位且包含大写、小写、数字、符号四类字符，且不得与最近 5 次旧密码相同。
- 忘记密码：联系租户管理员（tenant_admin）或平台管理员（admin）重置——重置后同样强制首登改密。
- 「设置」页可：修改密码、查看/登出全部在线会话、绑定 SSO（若启用）。

## 2. 提交作业（作业管理页）

1. 填写作业名、分区（standard=E 核默认 / performance=P 核 GPU）、节点/任务数、时限（分钟）、可选内存 MB 与 GPU 卡数，粘贴作业脚本。
2. **常用模板**一键填充：CPU 小任务 / 单卡 PyTorch / 长时批处理——套用后按需修改再提交。
3. 高级选项支持数组作业（如 `1-10`）与依赖（如 `afterok:123`）。
4. 作业列表 5 秒自动刷新；行内可取消/挂起/重排。
5. 点**详情**看作业生命期（提交/开始/结束/耗时/退出码）与**输出末 200 行**——不用登录集群找日志。完整输出在集群侧 `/shared/jobs/<作业号>.out`。

> 提示：数据与代码放 `/shared`（节点间共享、面板可见）比放家目录更方便。

## 3. Web-IDE（交互式开发）

- 「Web-IDE」页启动 **JupyterLab** 或 **VS Code**（code-server），默认 2 小时（可延时，上限 12 小时）。
- 启动后直接在浏览器里写代码——环境在计算节点上，数据读写 `/shared` 即集群数据。
- 会话结束（到期/手动回收）后工作内容保留于 `/shared`。

## 4. 计费与配额（计费页）

- 展示你（或你所选范围）的 CPU/内存/GPU 小时用量与**估算费用**，当前费率一并列出（改价不发版，以页面展示为准）。
- 页面顶部展示**本租户并发资源上限**（如 CPU 32 核 · 内存 64G）——注意这是**同时在跑**的上限（GrpTRES），不是累计用量额度。
- 作业历史页可按状态/用户过滤检索全部历史作业。

## 5. API 令牌（脚本/cron 调用面板 API）

- 入口：右上角「安全设置」→ **API 令牌**。创建时填名称（如 `ci-deploy`）与有效天数（留空=长期），**明文令牌只显示一次，立即保存**。
- 用法：请求头带 `Authorization: Bearer ailspat_…` 即可调用全部面板 API（身份与权限同网页登录）：

```bash
TOKEN="ailspat_粘贴你的令牌"
curl -H "Authorization: Bearer $TOKEN" https://portal.nilpo.app/api/v1/slurm/jobs
```

- 令牌**不会**因改密/登出失效（与网页会话独立）；不再使用时在设置页**吊销**（立即生效）。每人最多 10 个活跃令牌。
- 安全提醒：令牌等同你的账号权限，只放服务器环境变量或密管工具，不要写进代码仓库。

## 6. 常见问题

| 现象 | 处理 |
|---|---|
| 首次登录提交作业被拒（提示需改密） | 按 §1 完成强制改密后即可 |
| 提交报 Invalid account | 新账号供给延迟极少发生；持续出现联系管理员 |
| IDE 启动后进不去 | 会话约需数十秒拉起；列表页刷新确认状态为运行中再点入 |
| 输出详情里"暂无输出" | 作业可能尚未开始运行，或 stdout 确为空 |
| 密码改完旧页面全 401 | 改密会使所有在线令牌失效——重新登录即可（这是特性不是故障） |

## 7. 找谁

- 作业/IDE/配额问题 → 本租户 tenant_admin
- 账号开通/重置/权限 → 平台 admin（面板「平台管理」页操作，全程留审计）
